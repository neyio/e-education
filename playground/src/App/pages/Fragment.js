import React from 'react';
import { router } from 'dva';
const { Link, Route } = router;
import BaseLayout from '../layouts';
export default function EntrancePage(props) {
	return (
		<div id="e-education">
			<BaseLayout>nothing</BaseLayout>
		</div>
	);
}
